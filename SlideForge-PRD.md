# SlideForge — 产品需求文档（PRD）

> 版本：v2.1 | 日期：2026-04-21 | 状态：Draft
> 更新日志：
> - v2.1 — 新增第 9 节项目实施路径（Phase 规划），含依赖关系、各阶段任务清单和工期估算
> - v2.0 — 极简部署方案：只需 Python + Node.js，零外部服务依赖
> - v1.3 — 非功能需求补充本地开发环境要求（Docker Desktop WSL2 后端、8GB+ 内存）
> - v1.2 — 根据评审反馈：设计师模式移除 AI 补全；节点级 AI 描述开关；移除素材库和模板库；备注映射为演讲者备注；网页端不做 PPT 编辑；自动模式大纲生成到画布；AI 模型简化为单一 GLM-4-FlashX
> - v1.1 — 思维导图改为内置一体化画布；AI 方案切换为智谱 GLM 全家桶

---

## 1. 产品概述

### 1.1 产品定位

SlideForge 是一款面向设计师的 **AI 辅助 PPT 制作平台**，核心差异化在于 **"AI 自由度可调"**——用户可在「完全手动」到「全自动」之间自由切换，精确控制 AI 对 PPT 结构与内容的介入程度。

### 1.2 目标用户

| 用户画像 | 核心诉求 |
|---------|---------|
| 平面/品牌设计师 | 保留对排版、配图的完全控制，用 AI 提升效率 |
| UI/UX 设计师 | 快速将思维导图转化为演示文稿 |
| 设计团队负责人 | 标准化团队产出，统一模板风格 |
| 独立创作者 | 低成本快速产出高质量演示 |

### 1.3 核心价值

- **可控性**：AI 是工具而非决策者，用户始终掌握最终控制权
- **灵活性**：同一平台覆盖从素材组装到全自动生成的全场景
- **一体化创作**：思维导图创建与资料整理在同一画布完成，边想边做
- **零 AI 成本**：基于智谱 GLM 免费模型，用户无 AI 调用费用

---

## 2. 三级控制模式

这是产品的核心特性。用户通过一个 **「自由度滑块」** 在三种模式间切换：

### 模式一：设计师模式（AI 自由度 = 0%）

> **"我提供一切，你只负责组装"**

| 输入项 | 必填 | 说明 |
|--------|------|------|
| 思维导图（画布创建） | ✅ | 在内置画布上创建节点，定义 PPT 结构 |
| 节点图片 | ✅ | 在每个节点上挂载对应图片 |
| 节点文字描述 | ❌ | 可选，有则排入 PPT，无则仅排图片 |
| PPT 模板 | ✅ | 用户上传的 .pptx 模板文件 |

**AI 行为**：
- **完全不介入内容生成**——不生成文案、不生成描述、不替换图片
- 严格按画布节点结构生成页面
- 将节点中的图片精确排入模板对应位置
- 有文字描述的节点 → 图片 + 文案一起排入
- 无文字描述的节点 → 仅排入图片
- 仅做格式适配（字号缩放、布局微调以适配模板）

### 模式二：协作模式（AI 自由度 = 50%）

> **"我提供骨架，你帮我填充"**

| 输入项 | 必填 | 说明 |
|--------|------|------|
| 思维导图（画布创建） | ✅ | 在画布上创建节点，定义 PPT 结构 |
| 主题/关键词 | ✅ | 描述演示主题 |
| 节点图片 | ❌ | 可选，缺失时 AI 自动配图 |
| 节点文字描述 | ❌ | 可选，缺失时可按节点选择是否让 AI 生成 |
| PPT 模板 | ✅ | 用户上传的 .pptx 模板文件 |

**AI 行为**：
- 按画布节点结构生成页面
- 自动联网搜集资料、生成文案（GLM-4-FlashX + 联网搜索）
- 自动配图（Pexels 图库搜索，用户可逐一替换）
- **每个节点独立控制**：用户可选择是否让 AI 为该节点生成描述；选择"否"则该节点仅排图片
- 用户可对 AI 生成的内容逐页审核和编辑

### 模式三：自动模式（AI 自由度 = 100%）

> **"给我一个主题，其余交给你"**

| 输入项 | 必填 | 说明 |
|--------|------|------|
| 主题描述 | ✅ | 一句话描述演示主题 |
| 偏好设置 | ❌ | 风格、页数、语言等 |
| PPT 模板 | ❌ | 可选，用户上传或使用默认模板 |

**AI 行为**：
- 自动生成大纲并**可视化到画布上**（用户可在画布上拖拽调整节点结构、增删章节）
- 用户确认画布结构后，AI 联网搜集资料并撰写全部文案（GLM-4-FlashX + 联网搜索）
- 自动配图（Pexels 图库搜索）
- 遇到数据/图表类内容时，使用固定样图并提示用户替换
- 生成后用户可全局编辑

### 模式对比总览

| 维度 | 设计师模式 | 协作模式 | 自动模式 |
|------|-----------|---------|---------|
| 结构控制 | 用户完全控制 | 用户定义骨架 | AI 生成到画布，用户可调整 |
| 内容来源 | 用户全部提供 | 混合（用户+AI） | AI 全部生成 |
| 图片来源 | 用户上传到节点 | 混合 | AI 搜索图库 |
| 模板来源 | 用户上传 | 用户上传 | 用户上传或默认 |
| AI 介入程度 | 仅排版组装，不生成任何内容 | 按节点选择是否 AI 补全 | 全流程生成 |

---

## 3. 核心功能模块

### 3.1 项目工作台

- **项目列表**：创建、管理、归档 PPT 项目
- **版本历史**：自动保存编辑快照，支持回滚
- **预览**：在网页端预览 PPT 效果（不可编辑，编辑需下载后进行）

### 3.2 一体化思维导图画布（核心模块）

> **不依赖外部导入，在平台内直接创建思维导图，同时完成资料整理**

- **节点创建**：拖拽创建节点，支持命名（如"场地文脉"）、层级嵌套、自由排列
- **资料挂载**：每个节点支持挂载：
  - 图片（上传，缩略图预览）
  - 文字描述（直接在节点内编辑，将排入 PPT 正文）
  - 备注（将填入 PPT 的演讲者备注栏）
- **AI 描述开关**：每个节点可独立设置是否允许 AI 生成描述（仅协作/自动模式生效）
- **可视化展示**：所有内容在画布上实时可视化——节点名称、图片缩略图、描述摘要一目了然
- **结构映射**：节点层级自动映射为 PPT 结构：
  - 根节点 → PPT 标题页
  - 一级节点 → 章节分隔页
  - 二级节点 → 内容页
  - 三级节点 → 内容页内的要点列表
- **灵活调整**：支持拖拽排序、展开/折叠、批量操作

### 3.3 AI 内容生成（智谱 GLM）

- **文案生成与联网搜集**：基于 GLM-4-FlashX（单一模型），支持联网搜索 + 文案生成 + 润色，一步完成
- **图片理解**：基于 GLM-4.6V-Flash（视觉模型），当节点开启 AI 描述且有图片但无描述时，结合图片内容 + 节点上下文自动生成描述
- **智能配图**：基于 Pexels API，按主题语义搜索免费高质量图片
- **大纲生成**：自动模式下，根据主题生成大纲并可视化到画布上，用户可拖拽调整后确认
- **数据图表处理**：大纲中涉及数据/图表的部分，使用固定样图占位并提示用户自行替换

### 3.4 模板管理

- **模板上传**：用户上传自定义 .pptx 模板文件
- **模板解析**：自动解析模板的版式、占位符、母版结构
- **默认模板**：系统内置 1-2 套基础模板，用户也可上传自己的

### 3.5 导出功能

- **PPTX 导出**：导出为标准 .pptx 格式，保留所有排版，用户下载后可在 PowerPoint 中编辑
- **PDF 导出**：导出为 PDF 用于分享
- **图片导出**：逐页导出为 PNG/JPG

---

## 4. 用户流程

### 4.1 设计师模式流程

```
创建项目 → 选择「设计师模式」
  → 上传 PPT 模板 → 系统解析版式
  → 在画布上创建思维导图节点
  → 为每个节点命名（如"场地文脉"、"气候条件"）
  → 上传图片到节点
  → 可选：在节点中输入文字描述和备注
  → AI 组装排版（严格按节点内容，不生成任何新内容）
  → 预览效果 → 导出 PPTX → 下载后在 PowerPoint 中微调
```

### 4.2 协作模式流程

```
创建项目 → 选择「协作模式」
  → 上传 PPT 模板 → 系统解析版式
  → 在画布上创建思维导图骨架（命名节点）
  → 输入主题关键词
  → 为每个节点选择：是否允许 AI 生成描述
  → 触发 AI 生成：
      → GLM-4-FlashX 联网搜集资料 + 生成文案（仅对开启 AI 的节点）
      → Pexels API 搜索配图（缺失图片的节点）
      → GLM-4.6V-Flash 处理有图无描述且开启 AI 的节点
  → 用户逐页审核/替换/编辑
  → 预览效果 → 导出 PPTX
```

### 4.3 自动模式流程

```
创建项目 → 选择「自动模式」
  → 输入主题描述 → 设置偏好（可选）
  → AI 生成大纲 → 可视化到画布上（思维导图形式）
  → 用户在画布上确认/调整结构（拖拽、增删节点）
  → 确认后触发 AI 填充内容：
      → GLM-4-FlashX 联网搜集资料 + 生成文案
      → Pexels API 搜索配图
      → 数据/图表部分 → 固定样图 + 提示用户替换
  → 预览效果 → 导出 PPTX
```

---

## 5. AI 服务方案

### 5.1 模型选型（全部基于智谱 GLM，零成本）

| 用途 | 模型 | 费用 | 说明 |
|------|------|------|------|
| 文案生成 / 联网搜集 / 润色 | **GLM-4-FlashX** | 永久免费 | 单一模型覆盖所有文本任务，内置 web_search 工具 |
| 图片理解（无描述时） | GLM-4.6V-Flash | 永久免费 | 视觉模型，理解图片内容+上下文生成描述 |
| 图片搜索 | Pexels API | 免费 | 海量高质量免费图库 |

> GLM-4-FlashX 是 GLM-4-Flash 的增强版，同时支持普通文本生成和联网搜索，无需两个模型分工。

### 5.2 AI 调用流程

```
协作/自动模式触发
  → GLM-4-FlashX（联网搜索 + 文案生成，一步完成）
  → Pexels API 按关键词搜索配图
  → GLM-4.6V-Flash 处理"有图无描述且开启 AI"的节点
  → 所有结果返回前端，由网站排版引擎组装到 PPT
```

### 5.3 成本估算

| 项目 | 单项目成本 |
|------|-----------|
| AI 文本生成 + 联网搜索 | ¥0 |
| AI 图片理解 | ¥0 |
| 图片搜索（Pexels） | ¥0 |
| **合计** | **¥0** |

> 限制：免费模型限 30 并发，MVP 阶段足够，后续可升级付费版。

---

## 6. 非功能需求

| 维度 | 要求 |
|------|------|
| 性能 | PPTX 导出 < 10s（20页以内） |
| 安全 | 用户上传素材隔离存储，AI 生成内容不用于训练 |
| 可用性 | 支持 Chrome/Safari/Edge 最新版 |
| 扩展性 | AI 模型可替换；预留网页端编辑器接口供后续扩展 |
| 多语言 | 首期支持中文和英文 |
| 开发环境 | Python 3.10+ + Node.js 18+；无需 Docker、数据库、Redis 等外部服务；SQLite 内置零配置 |

---

## 7. MVP 范围（v1.0）

### 包含

- ✅ 三级控制模式（核心流程）
- ✅ 内置一体化思维导图画布（创建节点 + 挂载图片 + 输入描述/备注）
- ✅ 节点级 AI 描述开关
- ✅ AI 文案生成与联网资料搜集（GLM-4-FlashX）
- ✅ AI 图片理解（GLM-4.6V-Flash，处理有图无描述节点）
- ✅ AI 智能配图（Pexels API）
- ✅ 自动模式大纲生成到画布
- ✅ 用户上传自定义模板 + 模板解析
- ✅ 系统默认模板（1-2 套）
- ✅ 网页端预览（只读）
- ✅ PPTX 导出（下载后编辑）
- ✅ PDF / 图片导出

### 不包含（后续迭代）

- ❌ 网页端 PPT 编辑器（当前仅预览，编辑需下载后进行；已预留接口）
- ❌ 实时多人协作
- ❌ 品牌套件管理
- ❌ 动画编辑器
- ❌ 图表数据可视化（当前用固定样图占位）
- ❌ 移动端适配
- ❌ 外部思维导图文件导入（.xmind、.mm 等）
- ❌ 素材库（用户直接上传到节点）
- ❌ 模板库/模板推荐（用户自行上传模板）

---

## 8. 成功指标

| 指标 | 目标值 |
|------|--------|
| 用户完成率 | ≥ 70% 的创建项目成功导出 |
| 平均制作时长 | 自动模式 < 3 分钟，设计师模式 < 15 分钟 |
| 用户满意度 | NPS ≥ 40 |
| AI 成本 | ¥0 / 项目（基于免费模型） |

---

## 9. 项目实施路径（Phase 规划）

### 依赖关系总览

```
P0 基础框架 ──→ P1 画布 ──→ P2A 模板 ──→ P3 预览 ──→ P5 导出
                 │
                 └──→ P2B AI ──→ P4 模式 ──→ P5 导出
```

> P2A 和 P2B 可并行开发；P3 和 P4 可并行开发；P5 依赖 P3 + P4。

### Phase 0：基础框架搭建

**目标**：项目骨架可运行，前后端通信畅通

| 任务 | 说明 |
|------|------|
| 前端路由 + 布局 | React Router 配置、Header/侧边栏/主内容区布局 |
| 后端基础框架 | FastAPI 入口、CORS、配置管理、SQLite 连接、Alembic 迁移 |
| 用户认证 | 注册/登录/JWT（简单版，后续可扩展） |
| 项目 CRUD | 创建/读取/更新/删除项目 |
| 前后端联调 | Vite Proxy + axios 请求封装，确保 /api 通信正常 |

**交付物**：能登录、能创建项目、前后端通信正常

---

### Phase 1：一体化思维导图画布

**目标**：核心画布可用，节点可增删改

| 任务 | 说明 |
|------|------|
| React Flow 画布 | 画布初始化、缩放/平移、自定义节点渲染 |
| 资料节点组件 | 图片缩略图 + 描述 + 备注 + AI 开关 |
| 章节节点 / 根节点 | 不同节点类型的渲染和交互 |
| 画布工具栏 | 添加节点、删除节点、自动布局 |
| 节点属性面板 | 侧边栏编辑描述/备注/管理图片/AI 开关 |
| 图片上传 | 本地文件上传到 data/uploads/images/ |
| 画布数据持久化 | 节点增删改同步到后端 SQLite |

**依赖**：P0（需要项目 CRUD 和认证）
**交付物**：能创建项目、在画布上添加/编辑/删除节点、上传图片

---

### Phase 2A：模板上传与解析

**目标**：用户能上传 PPT 模板，系统解析其布局

| 任务 | 说明 |
|------|------|
| 模板上传接口 | 上传 .pptx 文件到 data/uploads/templates/ |
| 模板解析引擎 | python-pptx 解析模板的版式、占位符、母版 |
| 模板管理 | 模板列表、默认模板（1-2 套内置） |

**依赖**：P1（需要画布和项目框架）
**交付物**：能上传和管理 PPT 模板

### Phase 2B：AI 引擎集成

**目标**：AI 功能可用，节点描述可自动生成

| 任务 | 说明 |
|------|------|
| 智谱 API 客户端 | GLM-4-FlashX 文案生成 + GLM-4.6V-Flash 图片理解 |
| Pexels 图片搜索 | 关键词搜索 + 结果展示 |
| 节点 AI 描述生成 | 单节点/批量生成描述（BackgroundTasks 异步） |
| 自动模式大纲生成 | 主题 → 大纲 → 画布节点结构 |
| AI 交互面板 | 生成进度展示、结果审核与确认 |

**依赖**：P1（需要画布节点数据结构）
**可与 P2A 并行开发**
**交付物**：AI 能为节点生成描述、能自动生成大纲到画布

---

### Phase 3：PPT 预览

**目标**：画布内容可预览为 PPT 样式

| 任务 | 说明 |
|------|------|
| 结构映射 | 画布节点层级 → PPT 页面结构 |
| 幻灯片渲染组件 | 按模板版式渲染每页内容（标题/正文/图片/备注） |
| 页面导航 | 翻页、缩略图导航 |
| 备注 → 演讲者备注 | 节点备注映射到 PPT 备注栏 |

**依赖**：P2A（需要模板引擎）
**交付物**：能以 PPT 样式预览画布内容

### Phase 4：三级控制模式

**目标**：自动模式和设计师模式完整可用

| 任务 | 说明 |
|------|------|
| 模式选择器 | 自动模式 / 设计师模式切换 |
| 自动模式完整流程 | 主题输入 → AI 大纲 → 画布 → AI 配图 → 预览 |
| 设计师模式完整流程 | 手动创建节点 → 上传素材 → 编辑描述 → 预览 |
| 结构映射完善 | 节点层级 → PPT 章节结构的完整映射规则 |

**依赖**：P2B（需要 AI 引擎）
**可与 P3 并行开发**
**交付物**：两种模式的完整流程都能走通

---

### Phase 5：导出功能

**目标**：能导出 PPTX/PDF/图片

| 任务 | 说明 |
|------|------|
| PPTX 导出 | python-pptx 按模板填充内容，生成 .pptx 文件 |
| PDF 导出 | PPTX → PDF 转换 |
| 图片导出 | 每页导出为 PNG 图片 |
| 导出进度 | BackgroundTasks 异步导出 + 前端轮询状态 |

**依赖**：P3（预览）+ P4（模式）
**交付物**：完整的端到端流程（创建 → 编辑 → 预览 → 导出）

---

### 实施节奏建议

| 阶段 | 预计工期 | 累计进度 |
|------|---------|---------|
| P0 基础框架 | 3-5 天 | 骨架可运行 |
| P1 画布 | 5-7 天 | 核心交互可用 |
| P2A 模板 + P2B AI（并行） | 5-7 天 | AI + 模板就绪 |
| P3 预览 + P4 模式（并行） | 5-7 天 | 完整流程可演示 |
| P5 导出 | 3-5 天 | MVP 完成 |
| **合计** | **约 3-4 周** | |
